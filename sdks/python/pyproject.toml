[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "memnox"
version = "0.1.0"
description = "Python client for the Memnox runtime - check every AI action before it executes."
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["memnox"]
