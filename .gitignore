node_modules/
dist/
coverage/
*.log
.DS_Store
.env
.env.*
!.env.example
.memnox/
__pycache__/
*.pyc
sessions/

# Encryption keyrings. Losing one loses every record written under it —
# back them up somewhere that is not this repository.
memnox-keyring*.json
*.keyring.json

# Written by the release workflow when it resolves the publish order.
publish-order.txt
