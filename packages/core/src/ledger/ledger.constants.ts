/**
 * Two agents in one file inside this window are working at the same time. Long enough
 * that a pause for a test run does not end a collision, short enough that yesterday's
 * edit is not one.
 */
export const DEFAULT_COLLISION_WINDOW_MINUTES = 30;

/** One shared file is a coincidence; two is a reason to ask somebody. */
export const MINIMUM_SHARED_TARGETS = 2;
