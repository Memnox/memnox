/**
 * The session a row is filed under when nothing named one, so a command run outside
 * `memnox run` still lands in the ledger rather than under an empty id.
 */
export const UNNAMED_SESSION = 'ses_local';
